<%= form_for(@slider) do |f| %>
  <% if @slider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slider.errors.count, "error") %> prohibited this slider from being saved:</h2>

      <ul>
      <% @slider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :slide %><br>
    <%= f.file_field :slide %>
  </div>
   <div class="field">
    <%= f.label :more_url %><br>
    <%= f.text_field :more_url %>
  </div>
   <div class="field">
    <%= f.label :article %><br>
    <%= f.text_field :article%>
  </div>
   <div class="field">
    <%= f.label :number %><br>
    <%= f.select :number, Slider::NUMBERS, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>